{
  "name": "blast-radius",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "blast": "dist/src/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "npm run build && node --test dist/test/issue-2.acceptance.test.js"
  },
  "dependencies": {
    "typescript": "^5.9.2"
  },
  "devDependencies": {
    "@types/node": "^24.3.0"
  }
}
